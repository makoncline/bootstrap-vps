Use the `napkin` skill once at the first message of each new repo session when that skill exists for the current environment.

Read `~/.codex/memories/machine-notes.md` at the start of work when the task may depend on machine-specific setup, tools, or conventions.

If you learn a non-obvious, reusable detail that would make future agents faster or less error-prone on this computer, append it to `~/.codex/memories/machine-notes.md` as:

- `[YYYY-MM-DD] Short label: What was learned. When it matters. What future agents should do.`
